// Aggregate file: keep stable import path, tests are split into smaller scenario files.

import "./workspace-settings/qq-channel.scenario";
import "./workspace-settings/editor-modal.scenario";
import "./workspace-settings/cards.scenario";
